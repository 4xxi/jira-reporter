<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">JIRA logs</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
            <li><a href="{{ path('homepage') }}">Dashboard</a></li>
            
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Reports <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('time_report', {'period': 'today' }) }}">Today</a></li>
                    <li><a href="{{ path('time_report', {'period': 'yesterday' }) }}">Yesterday</a></li>
                    <li><a href="{{ path('time_report', {'period': '7days' }) }}">7 days</a></li>
                    <li><a href="{{ path('time_report', {'period': 'current_week' }) }}">Current week</a></li>
                    <li><a href="{{ path('time_report', {'period': 'last_week' }) }}">Last Week</a></li>
                    <li><a href="{{ path('time_report', {'period': '30days' }) }}">30 days</a></li>
                    <li><a href="{{ path('time_report', {'period': 'last_month' }) }}">Last Month</a></li>
                    <li><a href="{{ path('time_report', {'period': 'current_month' }) }}">Current month</a></li>
                </ul>
            </li>
            
            </ul>
            <form class="navbar-form navbar-right">
            <input type="text" class="form-control" placeholder="Search...">
            </form>
        </div>
    </div>
</nav>
